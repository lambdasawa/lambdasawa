#!/bin/bash

set -eu

pushd "$PWD" >/dev/null 2>&1
builtin cd "$(reporoot)"

echo -n "$PWD/" && fd | grep package.json | sed 's/package.json//' | awk '{print "./" $0}' | filter-file

popd >/dev/null 2>&1
